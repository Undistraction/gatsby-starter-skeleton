// import isNotObject from 'ramda-adjunct';
// import { throwInvalidFontMapError } from '../errors';

// export default fontMap => {
//   if (isNotObject(fontMap)) {
//     throw new Error('');
//   }

//   return (name, { weight, style }) => {};
// };

// theme.fonts('default', normal, italic );

// theme.fonts.weightOf('default', 'bold'); // 300;
// theme.fonts.face('normal'); // Example, sans-serif;
// theme.fonts('default', 'italic')

// font-face: Example, sans-serif
// font-weight: 300,
// font-style: normal
